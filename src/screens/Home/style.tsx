import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
    container: {
        paddingTop: 20,
    },
    suggestion: {
        marginTop: 27,
    },
    trendingContainer: {
        marginTop: 32,
    },
});

export default style;
